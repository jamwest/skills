# Skills are agent-agnostic by default

Skills in this repo are written to work in any agent that can follow prose instructions — not tied to Claude Code or any other specific runtime. Where a skill must depend on agent-specific capabilities, it will be separated from the default set rather than mixed in. The exact mechanism for that separation (directory layout, naming convention, etc.) is not yet decided.
